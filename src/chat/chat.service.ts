import { Injectable } from "@nestjs/common";

@Injectable()
export class ChatService {
  // Placeholder — room/message persistence, event bus emission, etc.
  // Will be implemented when real-time chat features are built out.
}
