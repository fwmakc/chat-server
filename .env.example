NODE_ENV=development
IP=localhost
PORT=3004

# Auth server (JWT verification via JWKS)
AUTH_SERVER_URL=http://localhost:3001

# Internal API key for service-to-service calls
INTERNAL_API_KEY=

# Event server (for event subscription)
EVENT_SERVER_URL=http://localhost:3005

# Documentation
SWAGGER_PREFIX=swagger
SWAGGER_PREFIX_REDOC=redoc
SWAGGER_TITLE=Chat Server API
SWAGGER_DESCRIPTION=WebSocket chat server
SWAGGER_VERSION=1.0

# Infrastructure (optional)
SENTRY_DSN=
SENTRY_ENV=localhost

# Logging
MORGAN_LOG_FORMAT=dev
