NODE_ENV=development
IP=localhost
PORT=3004

DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chat_server
DB_USER=root
DB_PASSWORD=1234
DB_SYNCHRONIZE=true
DB_LOG=true

# Auth server (JWT verification via JWKS)
AUTH_SERVER_URL=http://localhost:3001

# Event bus (Redis)
REDIS_HOST=localhost
REDIS_PORT=6379
